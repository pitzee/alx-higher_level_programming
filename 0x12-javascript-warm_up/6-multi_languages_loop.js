#!/usr/bin/node
//prints 3 lines: (like 1-multi_languages.js) but by using an array of string and a loop

let lan = ['C is fun','Python is cool','JavaScript is amazing'];
for (let i = 0; i <= 2; i++)
{
	console.log(lan[i]);
}
